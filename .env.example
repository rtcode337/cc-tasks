# compose.yaml が読み込む。実ファイル (.env) はコミットしないこと
GOOGLE_CLIENT_ID=xxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxxxxx
# ログインを許可する Google アカウント (1 件のみ)
ALLOWED_EMAIL=you@example.com
# openssl rand -base64 32 などで生成した長いランダム文字列
MCP_API_KEY=

# 公開 URL。サーバーの OAuth は未設定でもリクエストから自動導出するので任意。
# ただし Claude Code 側の .mcp.json が接続先 ${PUBLIC_BASE_URL}/mcp に使うので、
# MCP を使うなら設定して同じ値をシェルにも export しておくとよい。
#PUBLIC_BASE_URL=https://cctasks.example.com
